{
  "name": "msupportautomation",
  "version": "1.0.0",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "auth": "playwright test --project=setup",
    "mcp": "npx @playwright/mcp@latest --browser chrome --storage-state auth.json --ignore-https-errors --viewport-size 1280,720 --output-dir test-results/mcp"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "description": "",
  "devDependencies": {
    "@playwright/mcp": "^0.0.78",
    "@playwright/test": "^1.58.0",
    "@types/dotenv": "^6.1.1",
    "@types/node": "^25.9.1"
  },
  "dependencies": {
    "dotenv": "^17.3.1",
    "mail-listener2": "^0.3.1",
    "quoted-printable": "^1.0.1"
  }
}
